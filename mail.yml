-
    name: Test
    hosts: localhost
    tasks:
          - name: Get windows password  
            ec2_win_password:
                instance_id: i-07b703f2943c1067d
                aws_access_key: "{{ec2_access_key}}"
                aws_secret_key: "{{ec2_secret_key}}"
                security_token: "{{ ec2_token }}"
                region: us-east-1
            key_file: "~/aws/keypair/awx-key1.pem"
            register: win_pass
  
          - debug : msg="{{ result }}"
